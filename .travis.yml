language: python
python:
  - 2.7
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -qq gcc liblapack-dev
install:
  - pip install -r requirements.txt
script:
  - python --version
  - ./test_run.sh
